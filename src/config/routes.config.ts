export const root = "/";

export const routes = {
    register: root + "register",
    login: root + "login",
    resetPassword: root + "reset-password",
    updatePassword: root + "update-password",
    confirmCode: root + "confirm-code",
    privacyPolicy: root + "privacy-policy"
}